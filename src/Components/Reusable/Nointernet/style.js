import Styled from 'styled-components';

const NoInternetStyle = Styled.div`
    .NoInternetAlert {
        position: absolute;
        width: 100%;
        text-align: center;
        opacity: 0.9;
        z-index: 100;
    }
`;

export default NoInternetStyle;